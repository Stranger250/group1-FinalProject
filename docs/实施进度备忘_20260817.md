# 实施进度备忘（PRD-V2 执行中）

> 更新：2026-08-17（暂停点）。本文档用于暂停后恢复执行：**先读本文档，再继续**。
> 分支：`feat/shudao-safety-assistant`，仓库：`git@github.com:Stranger250/group1-FinalProject.git`

---

## 一、总体进度

| 里程碑 | 内容 | 状态 |
| --- | --- | --- |
| M0 | B1 图片可见性 + AI 识别保留 | ✅ 完成（commit `40b114d`，test_b1_fix 10/10） |
| M1 | O4 六类语料 >1000 条 + O5 分块参数化 | ✅ 完成（188 文件 / 4632 条入库，Chroma 5675 块，commit `5db31fb`） |
| M2 | O6 评测集 + baseline | ✅ 完成（100 条 query 已跑，baseline_v1.json 已存档，commit `5db31fb`） |
| M3 | O1 分类精细化 + O3 上报三段式 | ✅ 完成（commits `a8e8081`、`9708532`） |
| M4 | O7 回答依据分层 | ✅ 完成（commit `629562f` + 未提交的 rerank 修复，见下） |
| M5 | O8 考试工坊重做 | ✅ 完成（后端 `6ff26e8` test 21/21；前端 `e4c4750`） |
| M6 | O9 管理端 + O10 法规文档库页 + B2 日志上报 | 🔄 进行中（B2 后端已改未验证；O10/O9 未开始） |
| M7 | O11 文件上传 + O12 Word/PPT 生成 | 🔄 进行中（O11 后端部分已改未完成） |
| M8 | O13 安全员 + 数据隔离 | ✅ 完成（commit `c2d4e7c`，test_audit_flow 14/14） |
| 收尾 | 全量回归 + 提交 + PRD 验收勾选 | ⏳ 未开始 |

## 二、当前未提交的改动（暂停点工作区）

```
M backend/app/rag/retriever.py            # ⚠️ 必须提交：O7 rerank 排序键修复（t[2] 是 score，t[1] 是 content）
M backend/app/api/log.py                  # B2：POST /logs/report 预留接口（未验证）
M backend/app/core/config.py              # B2：oa_report_url 配置占位
M backend/app/schema/chat.py              # O11：ChatIn.file_context 字段
M backend/app/service/qa_service.py       # O11：stream_qa 接收 file_context
M backend/app/ai/prompts.py               # O11：prompt 注入【用户上传文档】
M backend/data/rejected_examples.json     # 测试污染数据（建议 git checkout 还原）
```

**恢复顺序建议**：
1. `git checkout backend/data/rejected_examples.json` 还原测试污染；
2. 提交 retriever.py 修复（**必须先提交，否则 eval_rag 会崩**：rerank 返回 (cid, content, score)，排序键应为 `t[2]`）；
3. 按下方「三、剩余工作」继续。

## 三、剩余工作（恢复后按此执行）

### 1) 完成 O11（AI 助手文件上传）——M7 前半
- [ ] `backend/app/api/chat.py`：chat 接口把 `payload.file_context` 传给 `stream_qa`（当前未接）；
- [ ] 新增 `POST /api/v1/chat/files`：multipart 上传 txt/md/pdf/docx（≤10MB，白名单 + 魔数校验），解析为纯文本（pdf→pdfplumber/PyMuPDF、docx→python-docx），**截断 8000 字**，返回 `{text, filename, chars}`；敏感词检查；
- [ ] 前端 `AiAssistant.vue`：输入框加「上传文件」按钮 + 文件列表（解析中/成功/失败，可删除），发送消息时带 `file_context`；
- [ ] 验证：上传 docx/pdf → 提问「根据文档 X 回答…」→ 回答引用文件内容。

### 2) 验证 B2（日志上报预留接口）——M6 前半
- [ ] 重建容器后调 `POST /api/v1/logs/report`（ADMIN）：返回 `{oa_report_url_configured:false, payload:{source,version,period,item_count,items}}`；`oa_report_url` 为空时不发外部请求；调用留审计 `logs_report`；
- [ ] 若通过，连同 O11 后端一起提交。

### 3) O10 法规文档库页（M6）
- [ ] 后端：`GET /api/v1/documents`（列表：文档名/类型/层级/条数/字数/状态/上传时间，筛选 type/tier/keyword，来自 knowledge_document + 统计）；`GET /documents/{id}`（元信息 + 正文预览，正文从 Chroma/MySQL chunks 聚合）；管理端 `POST /documents`（上传 txt/md/pdf/docx → 走建库管道 clean→parser→loader 增量入库）；`DELETE /documents/{id}`（停用/删除含分块）；
- [ ] 前端：用户端「法规文档库」页（浏览/筛选/详情预览）；管理端在配置中心加「添加文档」入口（上传 + 类型/层级/来源 + 状态流转）；
- [ ] 注意：knowledge_document/knowledge_chunk 表结构见 `backend/app/model/chat.py`；上传入库复用 `backend/app/rag/build/loader.py` 的管道（可先落 crawler_output 再增量 build）。

### 4) O9 管理端配置页（M6）
- [ ] 模型管理：读写 LLM/VISION 配置（base_url/api_key/model_name/vision_*）——**写配置存储**（新表或 .env 兼容层，注意不要明文落 git）；
- [ ] 知识库管理：文档上传/停用/删除（与 O10 打通）；
- [ ] RAG 分块策略管理：读写 `rag_parent_split_chars/rag_child_min_chars/rag_child_max_chars` 等（保存 → 触发重建确认）；
- [ ] 管理端菜单/页面（现有管理页在 `frontend/src/views/admin/`，菜单在 `router/menu.ts`）。

### 5) O12 Word/PPT 生成（M7 后半）
- [ ] 后端 `POST /api/v1/gen-doc`：类型 word/ppt + 需求描述 → 大纲生成（**DeepSeek 走现有 llm_client**，WPS API key 不可用即走降级路径，见 PRD O12「API 实测记录」）→ python-docx 渲染 Word / python-pptx 渲染 PPT（≥8 页）→ 返回下载；留审计 `doc_generate`；
- [ ] 前端：AI 助手页「生成文档」入口（类型选择/需求描述/大纲预览/确认下载）；
- [ ] WPS key（`apik:0:whpe8tie6nvprkdf.cdh66koyi4ae9wab8avc5yzgxlj21gia`）换 token 曾失败（400 UnknownError），**不要再次硬编码尝试**，走 DeepSeek 降级即可，待用户提供可用 key。

### 6) 收尾
- [ ] 全量回归：`test_overall(62)/test_boundary_security(109)/test_illegal_cases(56)/test_hazard_flow(20)/test_subjective(21)/test_concurrency/test_bugfix(19)/test_exam_e2e(38)/test_stats_excel_logs(17)/test_requests4(13)/test_audit_flow(14)/test_category_flow(8)/test_workshop_flow(21)/test_b1_fix(10)`（BASE_URL=http://127.0.0.1:8002/api/v1）；
- [ ] 浏览器冒烟：8080 前端各角色（员工/安全员/管理员）走一遍核心页面；
- [ ] PRD-V2 §2 各优化项验收标准勾选（docs/PRD-V2_优化版.md）；
- [ ] 最终提交推送。

## 四、运行环境备忘（恢复必读）

- 后端容器：`http://127.0.0.1:8002`（测试 BASE_URL 加 `/api/v1`；本机 8000 被旧进程 13280 占用勿动）；
- 前端：nginx 容器 8080，挂载 `frontend/dist`（**改前端只需 `npm run build`，无需重启容器**）；
- MySQL：`127.0.0.1:3306`，root/123456，库 `shudao`（容器经 host.docker.internal 复用）；
- 管理员：admin / Admin@123456；员工注册即用；
- 容器重建：仓库根执行 `docker compose -f backend/docker-compose.yml up -d --build backend`（每次后端改动都要重建；启动需等 40-60s 预热）；
- 数据库迁移：`cd backend && python scripts/init_db.py`（幂等）；
- 建库：`cd backend && python scripts/build_knowledge_base.py --force`（**CPU 单线程嵌入 5675 块约 6.3 小时**，中途勿 kill；临时脚本 `tmp_build.log` 看进度；py-spy dump --pid 可确认在推进）；
- 语料生成：`python scripts/gen_six_category_corpus.py`（crawler_output 188 文件已入库已提交）；
- 评测：`python scripts/eval_rag.py` → `data/eval/baseline_v1.json`（RAG v1.0：overall Recall@5=0.685 / MRR=0.621；1to1=0.900，1to2=0.561，1to3=0.294 —— 多块召回是后续优化点）；
- **git 提交必须在仓库根 `D:\code\2026\7_8月实训\shudao` 执行**（在 backend 下会路径错误）；PowerShell 把 git stderr 当错误（exit 1 = 正常）；
- 测试脚本在 `backend/scripts/test_*.py`，除 overall_test 外都支持 `BASE_URL` 环境变量。

## 五、PRD 关键口径（执行时对照 docs/PRD-V2_优化版.md）

- O8 权限模型：用户仅「添加题目（PENDING 待管理员审核）+ 查询题目（仅 APPROVED）」+ 自由组卷 + 发布考试给指定用户（昵称搜索/多选/可撤销）+ 被分享者直接作答/收藏副本（source_paper_id）+ Word 导出；管理员可直接增删改查题目并审核；
- O13：安全员 = 普通用户全部功能 + 隐患处理页（模拟实现）；普通用户仅见本人上报的隐患（后端强制）；
- O4 六类语料：安全生产法 / 建设工程安全生产管理条例 / 企业安全管理制度 / 安全操作规程 / 应急预案 / 典型事故案例分析，合计 >1000 条（已 4632 条达标）；
- O6 测试 query：100 条（50 对1 / 33 对2 / 17 对3）已入库 `backend/data/eval/queries_v1.json`；
- O12 WPS：接口可达（openapi.wps.cn/v7/sse/aippt/generate_slides_from_pxf_v2），apik 需先经 account.wps.cn 换 token，**当前 key 换 token 被拒（400 UnknownError，待用户确认 key 授权）**，实现走 DeepSeek 降级。

## 六、已完成功能速览（演示用）

- 隐患：上报三段式（图片→AI识别勾选保留→提交）、分类树（6 大类 36 子类）、安全员处理页、普通用户数据隔离、导出 Word 试卷；
- AI 助手：SSE 流式（30ms 平滑）、依据来源按 国家级/省级/更低级 分组、六类知识库问答；
- 考试工坊：用户组卷/发布考试给指定用户/作答/收藏/统计、管理端题库审核、Word 导出；
- 管理：审计日志查询、用户管理、角色（员工/安全员/管理员）。
