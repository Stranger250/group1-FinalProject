# 爬虫替换语料：任务备忘（O4 数据源重做）

> 更新：2026-08-17（暂停点 2）→ **2026-08-18 凌晨：全部完成（101 篇真实语料入库 + 基线重评 + 回归 + 已推送）**。
> **用户要求**：不能用生成的语料（company/sop/plan/case 四类），**使用爬虫去网上找真实数据**；尽量与建设/交通相关（蜀道公司）。
> 分支：`feat/shudao-safety-assistant`。本文档与 `docs/实施进度备忘_20260817.md` 配套（后者是全局进度）。

## ✅ 最终完成（2026-08-18 凌晨）

**六类语料全部为真实公开内容（共 101 篇 / 16261 条），生成语料 160 篇已移入 `crawler_output_generated_backup/`（gitignore，不推送）**：

| doc_type | 篇数 | 条数 | 来源 |
| --- | --- | --- | --- |
| law | 9 | 840 | 原有真实法规（保留） |
| regulation | 19 | 1247 | 原有真实法规（保留） |
| case | 53 | 6119 | 应急管理部官方事故调查报告 PDF（江西新余1·24、宁夏银川6·21、长沙4·29、阿拉善2·22、安阳11·21、北京长峰4·18、密云7·28、商洛7·19 + 整改评估）+ **江门应急局事故报告 21 篇**（潭江特大桥4·13坍塌、江鹤高速9·26高处坠落、沈海高速鹤山3·18、深岑高速4·22、中阳高速9·10、台山12·11坠车、台山污水管网11·15坍塌、开平11·16、江海礼乐5·26中毒、银湖纸业8·30等）+ 松桃 docx 6 篇 + 红海湾/汕尾 4 篇（含广汕铁路蜡烛山隧道）+ 泉州欣佳3·7（318条）+ 无锡9·28（151条）+ 包头1·18（41条） |
| plan | 12 | 1599 | gov.cn 公开预案全文（涪陵高速 67、浦东桥梁隧道 150、义乌桥梁隧道 131、济宁兖州公路工程 170、台山 354、苏州 212、川沙 147、长治热力 162、国家总体预案 82、长宁 103、安阳 11、重庆 10） |
| company | 5 | 5892 | 宣城宁国化工园区制度汇编（82 章 1094 条）、**山西电建项目安全生产制度 PDF（4511 条）**、上海国资委监管办法 126 条、上海实施意见 36 条、**铁路营业线施工安全管理办法 125 条** |
| sop | 3 | 564 | 克孜勒苏动火作业安全管理规定 75 条、煤层气地面开采安全规定 431 条、化工过程安全管理指导意见 58 条 |

**建库提速（已提交 `4df37f3`）**：`_merge_short_articles` 合并企业/预案/案例类短条款（仅 doc_type ∈ {case, plan, company, sop}，法规条号语义保留），块数 17545→7514（-57%），全量重建约 1.5 小时（原 6-10 小时）。

**知识库重建结果（2026-08-18 凌晨完成）**：
- 101 篇文档 / 7100 文章 / 7514 块；MySQL = Chroma = 7514 一致；空间 cosine；交叉引用 167/172（97.1%）；
- 抽样 Recall@5 16/20；sub-block 指针正常；构建断言全部 PASS（文档≥100、章≥400、条≥3000、六类齐全）。

**评测基线（真实语料，`data/eval/queries_v2.json` → `baseline_v1.json`）**：
- overall n=100：**Recall@5=0.825 / MRR=0.790 / NDCG@5=0.810**（50 单块 / 33 双块 / 17 三块）；
- 1to1 Recall 0.860 / 1to2 0.803 / 1to3 0.765；
- 注：旧 `queries_v1.json` 的 expect 标题指向已删除的生成文档，导致旧基线 0.02 失真；v2 已全部重映射到真实语料标题。
- 已知检索弱点（基线记录，后续迭代可优化）：包钢稀土（q044/q097）、国家总体预案（q070）等查询因"爆炸/预案"高频词干扰未能进 top-5。

**回归**：核心测试套件（test_bugfix / test_new_features / test_b1_fix / test_hazard_flow）全绿，功能无破坏。

**提交与推送**：全部已提交并推送到 `feat/shudao-safety-assistant`（HEAD `3427710` 为阈值+评测集 v2+基线；此前 `4df37f3` 建库提速、`8de8e3b` 真实语料扩充）。

**爬虫脚本**（均已提交，可复跑）：
- `backend/scripts/crawler_mem_pdf.py` / `crawler_mem_bulk.py` / `crawler_mem_seeds.py` — 应急管理部官方报告（列表分页 zfxxgkpt + PDF 附件 pdfplumber 提取）
- `backend/scripts/crawler_gov_plan.py` — gov.cn 预案（种子 URL 直抓，通用正文提取）
- `backend/scripts/crawler_gov_company_sop.py` — gov.cn 制度/规程
- `backend/scripts/crawler_jiangmen.py` — 江门应急局事故报告栏目（21 篇建设/交通类）
- `backend/scripts/crawler_songtao_docx.py` — 松桃 gov docx 附件（6 篇）
- `backend/scripts/crawler_seeds_bulk.py` — 通用种子爬虫（PDF/docx/HTML 自动识别）
- `backend/scripts/crawler_safehoo.py` — 备选源（大量会员截断，仅补充）

**源探测结论（记录备用）**：
- safehoo.com：部分文章全文完整，大量被 VIP 截断 → 需按段落数过滤，仅作补充源；
- mem.gov.cn 列表页 JS 渲染不可爬 → zfxxgkpt 分页（HTML）可爬列表，正文为 **PDF 附件**，pdfplumber 提取成功率高；
- jiangmen.gov.cn 事故报告正文在 `/attachment/...pdf`（页面仅 1 段）；
- 66law/zhulouren/aqsc.cn/wikisource/晋安：404/下载站/拒绝/扫描件，弃用。

## 待做清单（全部完成）

- [x] **1. 重建完成**：`python scripts/build_knowledge_base.py --force`（101 篇 / 7514 块，约 1.5h，日志 `backend/tmp_build_real.log`）；
- [x] **2. 重评测**：`python scripts/eval_rag.py --queries data/eval/queries_v2.json`（旧 queries_v1 引用已删除生成文档，v2 重映射真实语料）；
- [x] **3. 更新 PRD**：docs/PRD-V2_优化版.md O4 数据来源说明改为「爬虫抓取真实公开内容」（已提交 `8de8e3b`）；
- [x] **4. 回归**：核心测试套件全绿；
- [x] **5. 提交推送**。

## 四、恢复必读（技术备忘）

- 爬虫运行：`cd backend && python scripts/crawler_xxx.py`（种子在脚本内 SEEDS 列表，抓取幂等——文件已存在自动跳过；限速 0.4-1s）；
- 输出：`crawler_output/{标题}.json`（统一 schema：title/doc_type/category/doc_level/region/source/source_url/publish_date/effective_date/status/chapters）；
- PDF 提取：`pdfplumber`（已装），`pdf_to_paras` 按行合并为段落；
- 生成脚本 `gen_six_category_corpus.py` 与 `scripts/corpus_data/` **保留**（防回退），产物在 `crawler_output_generated_backup/`（已 gitignore，不推送）；
- 建库耗时经验：CPU 单线程嵌入，**中途勿 kill**；py-spy dump --pid 可确认推进；
- git 提交必须在仓库根执行。

---

## 一、任务目标

把 `crawler_output/` 中由 `gen_six_category_corpus.py` **生成**的四类语料（企业制度 40 / 规程 38 / 预案 30 / 案例 52 篇），替换为**爬虫抓取的真实公开内容**：

| doc_type | 真实来源方向 | 数量目标 |
| --- | --- | --- |
| case 典型事故案例 | 官方事故调查报告（应急管理部/省应急管理厅） | ≥60 篇 |
| company 企业安全管理制度 | 政府/企业公开的制度、责任制、职责文本 | ≥50 篇 |
| sop 安全操作规程 | 公开的安全技术/操作规程文本 | ≥40 篇 |
| plan 应急预案 | 政府公开的应急预案全文 | ≥30 篇 |

六类合计 >1000 条（law/regulation 28 部真实法规保留不动）。

## 二、已完成探测（结论）

1. **safehoo.com（安全管理网）**：
   - ✅ 可访问，栏目丰富：`/Case/`（事故案例 220+ 链接）、`/Manage/System/`（制度）、`/Manage/duty/`（职责）、`/Tech/*`（规程技术）；
   - ❌ **部分文章正文被会员截断**（如湖南长沙 4·29 报告只抓到 2 段、页面仅 3 个 `<p>`）；但有**全文完整**的文章（内蒙古包头稀土爆炸调查报告 → 4 章 41 条 ✅、北京密云暴雨调查 43 条 ✅）；
   - 结论：safehoo 可用但需**按文章长度过滤**（正文段落 <5 段的丢弃），抓取时设置 `--limit` 放大样本量来筛出完整文章；
   - 爬虫已写好：`backend/scripts/crawler_safehoo.py`（列表翻页 + 正文提取 + 分条 + 幂等 + 限速），已试跑产出 6 篇 case（194-188 个文件）。

2. **应急管理部 mem.gov.cn 事故报告专栏**：列表页 JS 渲染（href 抓不到条目），**不适用直接爬列表**；但报告全文页质量极高（官方调查结论）。可行替代：搜索引擎收集报告页 URL 直抓，或放弃该源。

3. **四川省应急管理厅（yjt.sc.gov.cn）**：栏目 URL 待确认（搜索只命中 docx 附件页），继续找。

4. **66law.cn 合同范本**：404（需会员/反爬），放弃。

5. **zhulouren.com**：资源下载站（zip），非正文，放弃。

## 三、待做清单（恢复后按序执行）

- [ ] **1. 确认 case 主源**：试四川/其他省应急管理厅事故调查栏目（`yjt.sc.gov.cn` 站内搜索"事故调查报告"），或直接批量抓 safehoo `/Case/` 全部栏目（limit 大 + 长度过滤，估计可筛出 60+ 篇完整报告）；
- [ ] **2. 抓 company/sop**：safehoo `/Manage/System/` + `/Manage/duty/` + `/Manage/Edu/` + `/Tech/*`（同样按长度过滤）；
- [ ] **3. 抓 plan**：safehoo 无预案栏目 → 改用政府网站公开预案（web_search 收集 gov.cn 预案全文页 URL，种子列表直抓；或四川省政府/应急厅预案公开栏目）；
- [ ] **4. 清洗与分条**：现有 `parse_article/split_into_chapters` 已可用（段落→章/条）；补充：过滤 `<5` 段的文章、去广告段、标题清洗；
- [ ] **5. 替换生成语料**：删除 crawler_output 中 doc_type ∈ {company,sop,plan,case} 且 source=生成脚本 的文件（即 `gen_six_category_corpus.py` 产物，可先备份到 `crawler_output_generated_backup/`）；
- [ ] **6. 重建知识库**：`cd backend && python scripts/build_knowledge_base.py --force`（**CPU 嵌入约 6 小时**，后台跑勿 kill）；
- [ ] **7. 重评测**：`python scripts/eval_rag.py`（queries_v1 无需改）对比 baseline_v1；
- [ ] **8. 回归 + 提交**：测试套件全绿后提交；更新 `docs/实施进度备忘_20260817.md` 与 PRD O4 数据来源说明（改为"爬虫抓取真实公开内容"）。

## 四、恢复必读（技术备忘）

- 爬虫运行：`cd backend && python scripts/crawler_safehoo.py --section case --limit 100 --sleep 0.5`（各栏目分开跑；**文章正文 <5 段直接跳过**需先加进 `parse_article` 或 `save_doc` 判断）；
- 输出目录：`crawler_output/{标题}.json`（统一 schema：title/doc_type/category/doc_level/region/source/source_url/publish_date/effective_date/status/chapters[{chapter, articles:[{no,content}]}]）；
- 分条口径：章节标题行（"事故概况/直接原因/责任追究/整改措施/第一章…"等）作 chapter，正文段落作"第N条"（100-400 字）；
- 长度过滤阈值建议：**总条数 <5 或 段落数 <5 的文档丢弃**（safehoo 截断文章特征）；
- 限速 0.5-1s/请求；单篇 fetch 超时 20s、重试 1 次；
- safehoo 链接格式：`/Case/Case/{栏目}/{年}{月}/{id}.shtml`（正则已适配）；
- 生成脚本 `gen_six_category_corpus.py` 与 `scripts/corpus_data/` **保留不删**（防回退），但产物不再入库；
- 已入库（Chroma 5675 块/MySQL 188 文档）在重建前保持现状，不影响其他功能。

## 五、当前工作区状态（暂停点）

- 新增：`backend/scripts/crawler_safehoo.py`（已提交？→ 尚未提交，见 git status）+ crawler_output 新增 6 篇真实 case（包头爆炸报告等，质量好，可保留）；
- 未提交：`backend/scripts/crawler_safehoo.py`、`crawler_output/` 6 篇新 case 文件；
- 上一暂停点（docs/实施进度备忘_20260817.md）所述 B2/O11 半成品已提交（wip 提交 `49416d3`），不受本次任务影响。
