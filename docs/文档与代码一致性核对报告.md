# 文档与代码一致性核对报告

> 版本：v1.0 · 日期：2026-08-14
> 核对方式：11 个独立核对代理逐份读取文档全文 + 代码实证（read/grep），输出结构化差异清单后人工汇总。
> 范围：docs/ 下除「项目任务书-蜀道安全助手.md」（历史需求底稿，仅作背景，不参与核对口径）外全部文档。
> 用途：作为文档维护与验收的差异基线；各章节权威真源见文末「真源判定」。

---

## 0. 文档状态总览

| 文档 | 状态判定 | 总体一致度 | 关键偏差 |
| --- | --- | --- | --- |
| PRD.md | 权威真源（§1.3 范围 / §2 技术栈两节已过时） | 功能验收 §9.1 全达成 | 范围声明被代码超额突破；技术栈多处偏离 |
| ARCHITECTURE.md | 设计稿（§3.1 已自裁决单栈） | 核心架构约七成落地 | Redis 零使用；ai_service 未独立；部署 §9 纯设计稿 |
| DATABASE.md | 部分过时（§9 建表 SQL 已过时） | 正文 §1-8 与 schema.sql 大体一致 | 5 处演进列未记录；3 处类型/枚举漂移；§7 索引漏项 |
| AI_SOLUTION.md | 部分过时 | E02 主流程按文档落地 | 重写语义相反；视觉识别已实现（非 V2 预留）；ping 未实现 |
| RAG优化方案.md | 已基本落地 | 核心链路与参数契约高度一致 | 置信度归一化实现替代 min-max；评测/缓存/压测未落地 |
| FRONTEND.md | 已基本落地（路由真源） | 20 条路由逐条一致 | 代码多 3 条路由；C8 时长禁改仅后端兜底 |
| TEST_PLAN.md | 纯设计稿/计划 | 事实断言准确、规划零执行 | pytest/vitest/CI/压测/评测集均未落地 |
| CRAWLER.md + 数据格式与建库注意事项.md | 已基本落地 | 数据断言全吻合 | formulation_unit 丢弃；ref_out 带版本后缀；targets.json 一处分源 |
| docs/_drafts/（7 份草稿） | 过程文档 | 1-知识库构建、4-AI出题落地最实 | 5-评估与迭代整体未落地 |

---

## 1. PRD.md

**状态：权威真源；§1.3（本期范围）与 §2（技术栈）两节已过时。**

### 已实现（验收满足）
- H01-H03 隐患上报/列表（筛选+排序+分页）/详情+时间线+管理员闭环
- A01-A03 RAG 问答（28 部法规、多轮≥5、来源引用、SSE meta→delta→done）、会话 CRUD、流式输出
- E01 四题型 CRUD、E02 AI 出题（≥5 题/批、人工审核、通过率口径）、E03 手动/AI 组卷、E04 在线考试（倒计时/自动保存/防切屏/超时交卷）、E05 自动阅卷+成绩单
- B01-B03 登录注册 JWT / 角色权限 / 上传白名单
- §9.1 功能验收全部勾选达成；P01-P10 页面清单全数落地

### 已偏离（代码超出文档）
| 条目 | 文档 | 代码实际 |
| --- | --- | --- |
| §1.3 范围声明 | A04/E06/E07/E08「本期不实现」 | 已全部实现（快捷提问、考试记录、出题统计、审核流） |
| §1.4 角色 | 管理员/普通用户两种 | 三种角色（新增 SAFETY=2） |
| §3.2 H01 | 仅文字+图片+等级 | 额外实现 AI 视觉识别 /hazards/analyze（未配置时 503 降级） |
| §3.4 E01 | 四种题型 | 第 5 种 SUBJECTIVE 解答题（要点包含判分） |
| §3.3 模块二 | 仅 A01-A03 | A07 回答反馈、查看原文 /ai/article |
| §7 数据模型 | message.sources/hazard.images/exam_record.answers 为 JSON 字段 | 独立表 message_source/hazard_image/exam_answer；密码列名 password 而非 password_hash |

### 未实现
- **Docker/docker-compose 一键部署**（§2/§9.2）；接口文档 docs/API.md 缺失；根 requirements.txt 缺失

### 技术栈偏离（§2 已过时，实现真源为代码）
- PyJWT → **python-jose**；requests → **httpx/openai SDK**；前端 JavaScript → **TypeScript**；Vue Router 4 → **vue-router ^5.2.0**；云端 Embedding → **本地 bge-large-zh**；LangChain 仅作 Embedding 包装（链全手写）

---

## 2. ARCHITECTURE.md

**状态：设计稿；核心架构约七成断言落地，§3.1 为文档内权威（已自裁决 FastAPI 单栈）。**

### 已实现
- 前后端分离（REST+SSE）、api→service→repository 三层分层、15 张表、JWT+bcrypt+RBAC、RAG 全链（BGE+Chroma+BM25+RRF+reranker+SSE）、三大模块接口、统一响应 {code,message,data}、AI 安全（拒答/grounding/敏感词）

### 未实现
- **Redis**：仅 config.redis_url 配置，全仓零调用（§2.1/§7 缓存职责与 key 设计全部未实现）
- **ai_service 独立服务**：无 /internal/* 接口；AI 内嵌 backend/app/ai + app/rag（§3.1 新决策，§2.1/§6 未同步）
- **部署**：无 docker-compose/Dockerfile/nginx（§9 纯设计稿）
- **审计日志**：仅隐患留痕；登录/派单/验收审计缺失
- **HTTPS**：无 TLS；CORS 全放开（注释注明生产收紧）
- core/logging.py、utils/sse.py、alembic/、repository/conversation_repo.py、service/ai_service.py 不存在（职责在别处）

### 已偏离
- 参数校验失败返回 **422**（文档称 400）
- 视觉识别已实现（文档称 V2 预留）
- api/ai.py 实为 E02 出题接口（文档注释称会话/chat——实际在 api/chat.py）
- 前端目录/路由命名与 §4.2/§4.3 不符（真源：FRONTEND.md）

---

## 3. DATABASE.md

**状态：部分过时；§9 建表 SQL 已过时，权威真源为 backend/database/schema.sql。**

### 一致
- 15 张表清单、正文 §5.2/§6.2/§6.5 字段表、§8 初始化角色与 admin、E02/E04/E05 演进列已同步进正文

### 文档未记录的演进列（文档过时）
- user.email / avatar；message.feedback（A07）；message_source.doc_id / article_no；question.rewrite_of / rewrite_feedback / rewrite_pending（生成列）；exam_paper.gen_mode / status；exam_record.state / cheat_count / submit_reason / ongoing_key（生成列）

### 类型/枚举漂移
- question.answer 与 exam_answer.user_answer/correct_answer 实为 **TEXT**（文档写 VARCHAR(64)/(255)）
- question.type 代码支持 **SUBJECTIVE**（文档仅 4 题型）
- message.status 代码为 INTERRUPTED（文档写 GENERATING）
- knowledge_document.type 语义：代码存法律类别（category），文档称文件类型（pdf/docx 等）；status=PROCESSING 全代码无写入

### 其他
- §7 索引汇总漏 idx_rewrite_of / uk_rewrite_pending
- exam_record.start_time 文档称「默认 NOW」但 DDL 无 server default（仅 ORM 兜底）

---

## 4. AI_SOLUTION.md

**状态：部分过时。**

### 已实现
- E02 出题主流程（检索→Prompt→LLM JSON→Pydantic 校验自动重试≤2 次→PENDING 批量入库→审核落 reviewer/review_note）
- 检索参数 rrf_k=60、rerank_top_n=5、0.30/0.45 置信度档位与 config 一致

### 已偏离
| 条目 | 文档 | 代码实际 |
| --- | --- | --- |
| §6.1 重写 | 产出修订版新题（rewrite_of 指向原题、独立新批次） | **就地替换**原题回 PENDING（不新建题/批次） |
| §7.4 视觉 | V2 预留，本期不实现 | vision.py 已实现并接入 H01 |
| 置信度归一化 | min-max | 相对理论峰值（top_rrf×(K+1)/2） |
| §4.1 top-k | 向量召回 20 | 50（vector_top_k） |
| 兜底话术 | 「暂无相关资料，请咨询安全管理员」 | 「抱歉，我没有在法规知识库中检索到…」 |
| 引用来源 | 按 LLM 实际引用写 message_source | 进入 Prompt 的全部检索块写入（grounding_score 打分） |

### 未实现
- SSE 60s ping（rag_stream_ping_sec=60 定义但从未引用）；vec_sim_floor=0.75 阈值未文档化；出题去重、寒暄意图判断

---

## 5. RAG优化方案.md

**状态：已基本落地（检索参数唯一真源）。**

### 一致（核心链路）
- 向量 Top-50 + BM25 Top-50 → RRF K=60 → Top-20 → 交叉引用展开 → bge-reranker Top-5
- 置信度三档（0.30 拒答 / 0.45 保守 / 全量）、vec_sim<0.75 拒答、SSE 事件序
- 建库断言：28 文件 / 199 章 / 2087 条 / 303734 字 / 引用 172 / 解析率≥90%（build_knowledge_base.py 与 _rag_data_audit.json 吻合）

### 已偏离
- 归一化实现用「相对理论峰值」（min-max 版 normalize_rrf 保留在 confidence.py 但未调用）
- SSE 端点字段：实际 POST /api/v1/ai/chat；meta 无 task_id、done 无 elapsed_ms
- 0.75 阈值文档未记载；config 注释称按 data/calibrate_refusal.py 标定，**该脚本在仓库中不存在**（阈值来源不可复现）

### 未实现
- SSE ping 保活 / 断线重连续推（断线仅落 INTERRUPTED）
- 评测集（eval/ 目录、3100+300+620、baseline.json）全缺
- 压测（Locust/k6、50QPS、首字 P95≤3s、检索 P95≤500ms）
- 多级缓存（L1-L4）全部未实现
- LLM 改写与同义词扩展（仅规则多轮改写）
- grounding 无脚注重生成 / embedding 余弦打标
- 多文档一致性校验、上位法加权（doc_level 未参与排序）
- 流式输出敏感词复检（仅入口预检）
- 保守模式仅收窄 max_tokens，无「单点结论+建议查阅原文」行为约束

---

## 6. FRONTEND.md（含 frontend_design/00-05）

**状态：已基本落地；§3 路由为唯一真源（router/index.ts 与其一致）。**

### 一致
- 20 条路由 name/meta.roles/hidden 逐条吻合；C1-C10/R1/F1/F2 裁决绝大部分落实；P01-P10 页面核心点均存在
- §7 后端配套（/exams/papers、/exams/records、/hazards sort/order）已落地
- F2 历史原文：后端已补 doc_id/article_no，前端按齐备动态置灰（文档标「可选先置灰」→ 已超前实现）

### 已偏离 / 未实现
| 条目 | 文档 | 代码实际 |
| --- | --- | --- |
| §3 路由 | 20 条 | 额外 /hazards/analyze、/profile、/admin/users 三条 |
| §3.1 meta | 含 menu 字段约定 | 全站未使用 meta.menu（侧栏由 menu.ts 常量生成） |
| §4 请求层 | analyze 超时 160s；503 视觉专用文案 | 实际 300s；503 用通用文案 |
| §2 组件 | components/common/*、MessageList、ReviewDialog 等 | 不存在，UI 内联在页面组件 |
| §2 store | modules/{chat,hazard,exam}.ts | 仅 chat.ts；hazard 在 store/hazard.ts；无 exam store |
| §0 C8 | 时长禁改前端同步禁用 | duration 永不禁用，依赖后端 400 兜底 |
| §6 P02 | 上报页内嵌 AiAnalyzePanel 单图识别 | 独立 /hazards/analyze 页批量识别 |
| §5 SSE | chatSSE(body,…) 固定 URL | chatSSE(url, body, …) + content-type 校验 |
| §6 P05 | rewritten_used 展示「已结合上文」标签 | 未渲染 |
| §6 P10 | 成绩单默认复用交卷响应 | 挂载时始终重新 GET result |

---

## 7. TEST_PLAN.md

**状态：纯设计稿/计划；其"代码事实"断言准确且仍成立，但规划的工程交付物全部未落地。**

### 断言仍成立（被代码证实）
- SSE 未发 ping 帧；Redis 仅配置未使用；main.py 无 catch-all 异常（500 缺口属实）；DATABASE.md §9 过时；前端无 vitest/playwright；仓库无 pytest/CI

### 已过时 / 未落地
| 条目 | 方案 | 现状 |
| --- | --- | --- |
| §0.2 现状盘点 | 仅 4 个 httpx 脚本 | 现有 13 个测试脚本（+3 个 CDP JS），仍是同一套手工模式，未向 pytest 迁移 |
| §1.2 pytest 工程 | tests/unit\|integration\|e2e\|perf + requirements-dev.txt | 无 tests/ 目录、无 pytest 引用 |
| §4 脚本迁移 | 拆分 fixture + 修硬编码 id | 未迁移；test_exam_e2e 仍硬编码 q1-q6/24/23 |
| §3.3 seed_test_users | 统一幂等账号脚本 | 不存在，各脚本零散注册 |
| §7 前端测试 | vitest/playwright | 无任何测试库 |
| §7 M6 CI | .github/workflows | 无 .github |
| §2.5/§8 压测评测 | load_test.py / eval_rag.py / calib_refusal.py | 全部不存在 |
| §8 接口清单 | 自建接口清单替代 API.md | 未落地 |

---

## 8. CRAWLER.md + 数据格式与建库注意事项.md

**状态：已基本落地。**

### 一致
- 四源注册表与 crawler/sources/ 对应；28 部 JSON 顶层 schema 核验通过
- 199 章 / 2087 条 / 303734 字 / 引用 172 与 _rag_data_audit.json 及 Chroma 落库（2544 块：2286 父 + 258 子，cosine 1024 维）完全吻合

### 已偏离
- 「条号无异常」不实：四川省生产经营单位安全生产责任规定存在重复第十七条（代码以 _dup2 后缀规避）
- formulation_unit（四川 11 部）在建库时被整体丢弃（文档称"有则存"）
- 引用扫描实为两种形态（跨法《…》第X条 18 处 + 同法裸第X条 154 处），文档只描述一种
- 验收只断解析率≥90%（found=0 时 rate=1.0 误判 PASS），**未真正断言 172**（EXPECTED_REFS 仅提示文案）
- 元数据键名 parent_chunk_id（契约写 parent_id）；ref_out 实际带版本后缀（如 aqscf_第八十一条_v2021）
- targets.json 中事故报告条例标 flk，实际走 moj 通道（按清单重跑无法复现）
- 管道文件名 clean.py/parse.py/tag.py/ref.py → 实际 clean.py/parser.py/ref.py/loader.py（打标在 parser.tag_blocks）

---

## 9. docs/_drafts/（7 份草稿）

**状态：过程文档。1-知识库构建 与 4-AI出题专项 落地最实；5-评估与迭代 整体未落地。**

| 草稿 | 落地度 | 核心缺口 |
| --- | --- | --- |
| 1-知识库构建 | 已基本落地 | 增量 upsert、企业文档管道、Milvus 迁移未实现 |
| 2-检索层优化 | 部分过时 | 四段流水线+引用展开落地；领域过滤/层级加权/缓存/评测集未落地；cos 剪枝与改写被改道 |
| 3-生成层优化 | 部分过时 | 硬约束 prompt/SSE/grounding 落地；语义缓存/超时熔断/流式复检/历史摘要/无脚注重生成未实现 |
| 4-AI出题专项 | 部分过时 | 生成-审核-重写-通过率统计落地；检索纯 BM25 偏离方案、SimHash 去重/干扰项自动校验/Excel/负样本回流未实现 |
| 5-评估与迭代 | 纯设计稿 | 评测集/MRR/NDCG/LLM-judge/baseline 门禁全缺；仅 A07 反馈与批次通过率落地 |
| 6-工程与性能 | 部分过时 | 并发背压+SSE 引用先返+冷启动预热落地；L1-L4 缓存/ping/限流熔断/Prometheus/压测全缺 |
| 7-完整性评审 | 部分过时 | 参数冲突/引用展开归属/查看原文/冷启动/权限过滤已收敛；多轮与性能验收、评测集、企业文档仍缺 |

---

## 10. 真源判定（文档维护优先序）

```
backend/database/schema.sql            ← 建表唯一真源（DATABASE.md §9、PRD §7 概览已过时）
docs/RAG优化方案.md + rag_config.py     ← 检索参数唯一真源（config.py 注释按此维护）
frontend/src/router/index.ts           ← 路由真源（与 FRONTEND.md §3 一致）
backend/app/** 代码行为                ← 重写语义、置信度归一化、视觉实现等实现真源
docs/FRONTEND.md                       ← 前端设计真源（§0 裁决已收敛）
docs/PRD.md                            ← 需求真源（§1.3 范围、§2 技术栈两节待修订）
docs/TEST_PLAN.md                      ← 测试方案（规划，未执行）
```

## 11. 主要待办建议（按价值排序）

1. **修订 PRD §1.3（范围）与 §2（技术栈）**：与代码现状对齐（超额实现项、TypeScript/python-jose/httpx、SUBJECTIVE）
2. **更新 DATABASE.md §9 建表 SQL**：与 schema.sql 对齐（或删除该节改引 schema.sql）
3. **更新 AI_SOLUTION 重写语义与视觉章节**：就地为权威（文档说"新题独立批次"已与实现矛盾）
4. **补齐交付物缺口**（若验收要求）：Docker 部署、审计日志、接口文档 API.md、评测集/压测基线
5. **补 data/calibrate_refusal.py 或修正 config 注释**：0.75 阈值来源不可复现
6. **修正 CRAWLER targets.json 分源**（事故报告条例 flk→moj）与建库验收断言（真正断言 172）
7. **修复 .env.example 误导注释**：Redis 无消费者，切屏计数存 MySQL
